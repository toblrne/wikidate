import React from 'react';
import './Input.scss'

const Input = ({ data }) => {
    return (
        <div className="input">
            <form>
                <input placeholder="Month" />
                
            </form>
        </div>);
}

export default Input;